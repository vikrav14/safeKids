class Message:
    # message_id (Primary Key)
    # sender_id (Foreign Key to User model or Child model)
    # receiver_id (Foreign Key to User model or Child model)
    # content (text of the message)
    # timestamp
    # is_read (boolean)
    pass

class Child:
    # child_id (Primary Key)
    # user_id (Foreign Key to User model - linking child to a parent)
    # name
    # device_id (unique identifier for the child's device)
    # battery_status (integer percentage)
    # created_at
    # updated_at
    pass
